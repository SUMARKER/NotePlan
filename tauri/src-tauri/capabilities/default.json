{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "主窗口默认能力：事件系统（vault:changed / theme:changed / menu:* 通知）",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
